$Id$

Général:

- Under Linux, use 'clone' as alternative to pthreads (library 'MTK').
- Under Linux, use 'sem_init' and consort (library 'SPH') and use them in 'MTX').
- Under Windows, test with Borland C++.
- Make extensive tests and adapt libraries under OSes from the Sourceforge compile farm.
- Use 'bso::sign__' instead of 'bso::sbyte__' if relevant.
- Handle the preprocessor warning occuring, foe example, with the 'IDXBTQ' library, with g++ 3.0.

CTN:
- Test and use if working 'mutable'.

SSNMNG:
- Use typedef instead of directly bso::... and handle 'BSO_' constant.

STR:
- Add 'Init' fucntion which takes a 'const char *'.

TXMPGH:
- Introduce an alias to 'str::string_' when used as a line (txmpgh::line ?).

XMLDBS:
- Add deleting of items.

XMLDCM:
- Add deleting of items.
