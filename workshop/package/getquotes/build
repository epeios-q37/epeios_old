# $Id$
mkdir workdir
cd workdir
export DIR=getquotes
GET=../../get_cvs

mkdir -p $DIR

$GET epeios $DIR stable/*.h
$GET epeios $DIR stable/*.cpp

$GET private $DIR stable/*.h
$GET private $DIR stable/*.cpp

$GET private $DIR tools/$DIR/*.cpp
$GET private $DIR tools/$DIR/Makefile


#../../create_pkg $DIR
tar --gzip -cf $DIR.tar.gz $DIR
